% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clonal_sim.R
\name{run_clonal_replicates}
\alias{run_clonal_replicates}
\title{Run N independent clonal simulations and summarise each}
\usage{
run_clonal_replicates(
  chromosome,
  allele,
  hotspot_pos,
  params = clonal_params(),
  N_replicates = 100,
  max_cells = 1000,
  max_time = 300,
  sample_cells = NULL,
  bin_length = 1e+06,
  n_cores = 1
)
}
\arguments{
\item{chromosome}{Chromosome to simulate (character, e.g. \code{"7"}).}

\item{allele}{Allele to track (\code{"A"}, \code{"B"}, or \code{"CN"}).}

\item{hotspot_pos}{1-based bin index of the position of interest.}

\item{params}{Parameter list from \code{clonal_params()}.}

\item{N_replicates}{Number of independent clonal evolutions to simulate.}

\item{max_cells}{Stopping criterion: maximum alive cells.}

\item{max_time}{Stopping criterion: simulation time limit.}

\item{sample_cells}{Number of cells to sample after simulation.  NULL keeps all.}

\item{bin_length}{Bin size in bp.  Default 1 Mb.}

\item{n_cores}{Number of parallel cores (uses \code{parallel::mclapply}
when > 1).}
}
\value{
A \code{tibble} with one row per replicate and columns for each
summary statistic plus \code{replicate}, \code{n_alive}, and \code{failed}.
}
\description{
Each replicate is an independent run of \code{simulate_clone()}.  The
simulation stops when \code{max_cells} are alive or \code{max_time} is
reached, then \code{sample_cells} cells are drawn using
\code{subsample_sim()}.  Failed simulations (e.g. all cells die) are
recorded with NA summary statistics and flagged with \code{failed = TRUE}.
}
