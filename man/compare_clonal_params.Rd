% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clonal_sim.R
\name{compare_clonal_params}
\alias{compare_clonal_params}
\title{Compare summary statistics across multiple parameter sets}
\usage{
compare_clonal_params(
  param_list,
  chromosome,
  allele,
  hotspot_pos,
  N_replicates = 100,
  max_cells = 1000,
  max_time = 300,
  sample_cells = NULL,
  bin_length = 1e+06,
  n_cores = 1
)
}
\arguments{
\item{param_list}{Named list of parameter lists, each from
\code{clonal_params()}.  Names become the \code{condition} column.}

\item{chromosome}{Chromosome to simulate.}

\item{allele}{Allele to track.}

\item{hotspot_pos}{1-based bin index of the position of interest.}

\item{N_replicates}{Number of independent replicates per condition.}

\item{max_cells}{Stopping criterion: maximum alive cells.}

\item{max_time}{Stopping criterion: simulation time limit.}

\item{sample_cells}{Number of cells to sample after simulation.  NULL keeps all.}

\item{bin_length}{Bin size in bp.}

\item{n_cores}{Cores for parallelism (applies within each condition).}
}
\value{
A \code{tibble} with all replicates across all conditions.  Columns:
\code{condition}, \code{replicate}, summary statistics, \code{n_alive},
\code{failed}.
}
\description{
Runs \code{run_clonal_replicates()} for each entry in \code{param_list} and
combines the results into a single tidy data frame.  The name of each list
entry becomes the \code{condition} column, making it easy to facet or
colour by condition in downstream plots.
}
\examples{
\dontrun{
results = compare_clonal_params(
  param_list = list(
    neutral    = clonal_params(positive_selection_rate = 0),
    selection  = clonal_params(positive_selection_rate = 2),
    bfb_only   = clonal_params(bfb_prob = 0.9, positive_selection_rate = 0),
    bfb_select = clonal_params(bfb_prob = 0.9, positive_selection_rate = 2)
  ),
  chromosome   = "7",
  allele       = "A",
  hotspot_pos  = 60,
  N_replicates = 100,
  max_time     = 20,
  sample_cells = 50
)
plot_clonal_comparison(results)
}
}
