% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clonal_sim.R
\name{clonal_params}
\alias{clonal_params}
\title{Build a parameter list for clonal simulations}
\usage{
clonal_params(
  bfb_prob = 0.5,
  amp_rate = 0.3,
  del_rate = 0.2,
  positive_selection_rate = 0,
  negative_selection_rate = 0,
  birth_rate = 1,
  death_rate = 0.1,
  lambda = 2,
  rate = 20,
  first_round_of_bfb = TRUE
)
}
\arguments{
\item{bfb_prob}{Relative probability of a BFB event per division.}

\item{amp_rate}{Relative probability of a focal amplification.}

\item{del_rate}{Relative probability of a focal deletion.}

\item{positive_selection_rate}{Multiplicative birth-rate boost for cells that
have gained a copy at \code{hotspot_pos}.  0 = neutral.}

\item{negative_selection_rate}{Multiplicative death-rate boost for cells that
have gained a copy at \code{hotspot_pos}.  0 = neutral.}

\item{birth_rate}{Base cell birth rate.}

\item{death_rate}{Base cell death rate.}

\item{lambda}{Mean number of genomic events per daughter cell per
division (Poisson).}

\item{rate}{Scale of focal amp/del events: segment length is
drawn from Exp(1/rate).}

\item{first_round_of_bfb}{If TRUE the founding cell already carries one BFB
event, mimicking a clone that initiated from a single BFB.}
}
\value{
Named list of simulation parameters.
}
\description{
Returns a complete parameter list with sensible defaults. Any argument can
be overridden.  Pass the result to \code{run_clonal_replicates()} or
\code{compare_clonal_params()}.
}
